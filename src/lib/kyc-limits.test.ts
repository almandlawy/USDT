import{describe,it,expect}from"vitest";import{checkKycLimit}from"./kyc-limits";const rules=[{level:2,currency:"USD",perOrder:10000,daily:25000}];describe("KYC limits",()=>{it("blocks per-order excess",()=>expect(checkKycLimit(11000,"USD",2,0,rules).reason).toBe("per_order"));it("blocks daily excess",()=>expect(checkKycLimit(9000,"USD",2,20000,rules).reason).toBe("daily"));it("allows inside caps",()=>expect(checkKycLimit(5000,"USD",2,1000,rules).allowed).toBe(true))});
