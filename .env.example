# === Public ===
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_LIVE_TRADING=false
NEXT_PUBLIC_KYC_INTAKE_ENABLED=false
NEXT_PUBLIC_PROOF_INTAKE_ENABLED=false

# === Supabase (public) ===
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# === Supabase (server only — never NEXT_PUBLIC_*) ===
SUPABASE_SERVICE_ROLE_KEY=

# === Trading / fulfillment locks (must remain false in this phase) ===
LIVE_TRADING=false
REAL_PAYMENTS_ENABLED=false
AUTO_FULFILLMENT_ENABLED=false
LEGAL_APPROVAL_REFERENCE=

# === Auth / CAPTCHA ===
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
# Test-only bypass — ignored when VERCEL_ENV=production
TURNSTILE_BYPASS_FOR_TESTS=false
# Operational attestation that Supabase Auth email/SMTP is configured
AUTH_EMAIL_PROVIDER_CONFIGURED=

# === Security (server only) ===
SECURITY_HASH_SECRET=
INTERNAL_HEALTH_TOKEN=
CSRF_ALLOWED_HOSTS=

# === Document intake (server authoritative; fail closed until legal/privacy readiness) ===
KYC_INTAKE_ENABLED=false
PROOF_INTAKE_ENABLED=false

# === Stripe (honest USDT product only; requires official crypto approval) ===
STRIPE_ENABLED=false
STRIPE_CRYPTO_APPROVED=false
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# === Zain Cash ===
ZAINCASH_ENABLED=false
ZAINCASH_ENVIRONMENT=uat
ZAINCASH_BASE_URL=
ZAINCASH_CLIENT_ID=
ZAINCASH_CLIENT_SECRET=
ZAINCASH_API_KEY=
ZAINCASH_SERVICE_TYPE=
ZAINCASH_WEBHOOK_SECRET=

# === e& money (manual by default — no invented API) ===
EAND_MONEY_ENABLED=false
EAND_MONEY_MODE=manual
EAND_MONEY_REQUEST_PHONE=
EAND_MONEY_QR_IMAGE_URL=

# === du Pay (manual by default — no partnership claim) ===
DUPAY_ENABLED=false
DUPAY_MODE=manual
DUPAY_REQUEST_PHONE=
DUPAY_QR_IMAGE_URL=

# === Bank transfer ===
BANK_TRANSFER_ENABLED=false

# === Market / quotes ===
MARKET_PROVIDER_PRIMARY=coingecko
MARKET_PROVIDER_SECONDARY=
MARKET_PROVIDER_API_KEY=
MARKET_PRICE_MAX_AGE_SECONDS=120
QUOTE_DEFAULT_EXPIRY_SECONDS=300
COINGECKO_DEMO_API_KEY=
USD_TO_IQD_RATE=1310
USD_TO_AED_RATE=3.6725
MARKET_PROVIDER_TIMEOUT_MS=4000

# === Contact / company (do not invent licence claims) ===
NEXT_PUBLIC_COMPANY_NAME=Gulf Gate
NEXT_PUBLIC_LEGAL_NAME=
NEXT_PUBLIC_COMPANY_REFERENCE=55896311472
NEXT_PUBLIC_TRADE_LICENSE_NUMBER=
NEXT_PUBLIC_VARA_LICENSE_NUMBER=
NEXT_PUBLIC_VARA_LICENSE_TYPE=
NEXT_PUBLIC_VARA_LICENSE_STATUS=
NEXT_PUBLIC_VARA_LICENSED_ACTIVITIES=
NEXT_PUBLIC_VARA_LICENSE_VALID_FROM=
NEXT_PUBLIC_VARA_LICENSE_VALID_TO=
NEXT_PUBLIC_COMPANY_ADDRESS=
NEXT_PUBLIC_SUPPORT_EMAIL=
NEXT_PUBLIC_PRIVACY_EMAIL=
NEXT_PUBLIC_SUPPORT_PHONE=
NEXT_PUBLIC_WHATSAPP_NUMBER=
NEXT_PUBLIC_WORKING_HOURS=

# Publish legal address / VARA only when verified against official documents
COMPANY_LEGAL_DETAILS_VERIFIED=false
# Admin draft only — not public until verified
COMPANY_VERIFIED_LEGAL_ADDRESS_DRAFT=Office 206, Al Mulinia Tower, Al Khabisi, Dubai, United Arab Emirates

# === Uploads ===
SIGNED_URL_TTL_SECONDS=60
MAX_UPLOAD_BYTES=10485760
ALLOWED_UPLOAD_MIME_TYPES=image/jpeg,image/png,application/pdf

# === Monitoring (optional) ===
# SENTRY_DSN=
# COMMIT_SHA=
# BUILD_TIME=

# === E2E / QA ===
# PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000

# === Local demo / seeds (Vercel Production rejects DEMO_MODE=true) ===
DEMO_MODE=false
SEED_ADMIN_EMAIL=admin@example.test
SEED_ADMIN_PASSWORD=
ALLOW_TEST_SEED=false
TEST_USER_PASSWORD=
